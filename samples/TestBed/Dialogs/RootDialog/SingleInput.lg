# ButtonForIntent
[Activity
    Attachments = ${json(fromFile('./ButtonToRoute_ValueRecognizer.json'))}
]

# SingleProperty
[Activity
    Attachments = ${json(fromFile('./SingleInput.json'))}
]

# SingleProperty.invalid
[Activity
    Text = Sorry, **${this.value}** does not work. I'm looking for a valid email address.. e.g. info@contoso.com.
    ${SingleProperty()}
]